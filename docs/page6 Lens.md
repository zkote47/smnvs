# Lens
# Основа
## Что такое LENS?

**LENS** — это мощная, бесплатная и открытая (Open Source) **IDE (Integrated Development Environment) для Kubernetes**. Если представить, что командная строка (`kubectl`) — это текстовый редактор для работы с Kubernetes, то LENS — это полноценная среда разработки с графическим интерфейсом.

Официальный сайт: [https://k8slens.dev/](https://k8slens.dev/)

Проще говоря, **LENS позволяет управлять кластерами Kubernetes через удобное окошко, а не только через команды в терминале.**

---

## Для чего он служит? Основные цели и преимущества

1.  **Визуализация:** Это главная "фишка" LENS. Вы видите дерево всех ваших ресурсов (ноды, поды, сервисы, деплойменты и т.д.) и связи между ними. Понимание, какой под каким деплойментом работает, как сервисы связаны между собой, становится интуитивно понятным.
2.  **Упрощение операций:** Больше не нужно запоминать сложные команды `kubectl`. Запуск, обновление, удаление, просмотр логов, подключение к терминалу пода — все это делается в пару кликов мышкой.
3.  **Мульти-кластерное управление:** LENS позволяет добавлять и легко переключаться между множеством Kubernetes-кластеров (например, продакшен, тестовый, локальный Minikube). Это огромное удобство для администраторов.
4.  **Мониторинг в реальном времени:** Встроенные дашборды показывают метрики использования CPU, памяти, сети для всего кластера, отдельных нод и подов. Вам не нужно отдельно настраивать Prometheus и Grafana для базового мониторинга.
5.  **Устранение неполадок (Troubleshooting):** Быстрый доступ к логам и оболочке (shell) внутри пода значительно ускоряет процесс поиска и исправления ошибок.
6.  **Безопасность:** LENS работает поверх вашего `kubeconfig`-файла. Это значит, что он использует те же права доступа, что и `kubectl`. Если у вас есть доступ к кластеру через `kubectl`, вы можете подключить его к LENS.

---

## Как пользоваться? Пошаговое начало работы

#### Шаг 1: Установка

1.  Перейдите на [официальный сайт](https://k8slens.dev/).
2.  Скачайте установщик для вашей ОС (Windows, macOS, Linux).
3.  Установите LENS как обычное приложение.

#### Шаг 2: Добавление кластера

Когда вы впервые запустите LENS, он будет пустым.

1.  **Локальный кластер (Minikube / Kind / Docker Desktop):**
    *   Обычно LENS автоматически находит локальные кластеры. Они появятся в списке "Catalog" или "Clusters".
    *   Просто нажмите на него, чтобы подключиться.

2.  **Удаленный кластер (например, в облаке):**
    *   Вам понадобится `kubeconfig`-файл от этого кластера. Обычно он находится по пути `~/.kube/config`.
    *   В LENS есть несколько способов добавить кластер:
        *   **Через файл:** Перетащите ваш `kubeconfig`-файл в окно LENS.
        *   **Вручную:** Нажмите на значок "+" (Add Cluster) и выберите способ. Самый простой — указать путь к файлу.
    *   LENS прочитает файл и отобразит все контексты (кластеры), которые в нем описаны. Выберите нужный.

#### Шаг 3: Навигация по интерфейсу

После подключения к кластеру вы увидите интерфейс, разделенный на несколько основных областей:

*   **Левая панель (Навигатор):** Здесь отображается иерархия ресурсов Kubernetes.
    *   **Cluster:** Общая информация о кластере (ноды, ресурсы).
    *   **Workloads:** Ваши основные рабочие нагрузки — Поды (Pods), Деплойменты (Deployments), Стейтфулсеты (StatefulSets) и т.д.
    *   **Configuration:** Конфигурационные сущности — Конфигмапы (ConfigMaps), Секреты (Secrets) и др.
    *   **Network:** Сервисы (Services), Ингрессы (Ingresses).
    *   **Storage:** Постоянные тома (Persistent Volumes) и заявки на них (Persistent Volume Claims).
    *   **Access:** Сервисные аккаунты (Service Accounts), Роли (Roles) и т.д.

*   **Центральная панель:** Здесь отображается список выбранных ресурсов (например, все поды в неймспейсе `default`).

*   **Панель деталей:** Когда вы выбираете конкретный ресурс (например, под), здесь появляется подробная информация о нем: метаданные, статус, условия, события и т.д.

#### Шаг 4: Выполнение основных операций

Давайте рассмотрим, как в LENS делаются вещи, для которых в CLI требуются команды.

*   **Просмотр логов пода:**
    1.  Перейдите в `Workloads` -> `Pods`.
    2.  Выберите нужный под из списка.
    3.  Нажмите на иконку журнала (📄) или найдите кнопку "Logs". Логи откроются в отдельной вкладке.

*   **Подключение к терминалу пода:**
    1.  Аналогично, выберите под.
    2.  Нажмите на иконку оболочки ( Shell) или кнопку "Terminal".
    3.  Откроется терминал, где вы можете выполнять команды внутри контейнера.

*   **Редактирование ресурса (например, деплоймента):**
    1.  Найдите нужный деплоймент в `Workloads` -> `Deployments`.
    2.  Нажмите на три точки (меню) рядом с ним и выберите "Edit".
    3.  Откроется редактор YAML. Вы можете внести изменения и нажать "Save". LENS применит изменения к кластеру.

*   **Описание ресурса (эквивалент `kubectl describe`):**
    *   Просто выберите ресурс, и вся подробная информация будет в панели деталей справа или внизу.

*   **Мониторинг:**
    *   Перейдите в раздел `Cluster`. Вы увидите дашборды с графиками загрузки CPU и памяти по нодам.

---
## Советы 

1.  **Начните с локального кластера.** Поднимите Minikube или используйте Kubernetes из Docker Desktop и потренируйтесь на нем.
2.  **Используйте LENS вместе с `kubectl`.** LENS не заменяет знание `kubectl` на 100%. Понимание основных концепций и команд все равно необходимо. LENS помогает *визуализировать* эти концепции.
3.  **Изучайте через исследование.** Кликайте на все ресурсы, смотрите, как они связаны. Если вы создали деплоймент через `kubectl apply -f deployment.yaml`, зайдите в LENS и посмотрите, какой под он создал, какие у него метки, какие события с ним происходили.
4.  **Обращайте внимание на события (Events).** В панели деталей для почти любого ресурса есть вкладка "Events". Это золотая жила для диагностики проблем (почему под не запустился, почему образ не скачался и т.д.).


---

# Практика

Устанавливать LENS на линукс - ошибка :)
Скачиваем готовым файликом версию для Windows. Чтобы подружить LENS с имеющимися кластерами нужно нажать на плюсик в разделе Local Kubeconfigs и добавить нужный файл `kubeconfig` (`~/.kube/config`)

